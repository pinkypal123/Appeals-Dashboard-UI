export interface FormData {
  id: string;
  taxYear: number;
  company: string;
  state: string;
  assessor: string;
  accountNumber: string;
  appealDate: string;
  appealBy: string;
  status: string;
  appealDeadline:string;
}